launch: bash -c 'cd /workspaces/actions/launch && ./script/server'
github: bash -c 'cd /workspaces/github && GITHUB_CODESPACES_CUSTOM_PORT=80 ./script/server'
# adn: bash -c 'cd /workspaces/actions/actions-dotnet/src && ./script/server && kubectl get pods -w' # could check if deployment is still in ready state, going to assume that being the case for now